# appaday-125-blockwork
An offline shift coverage scheduler. Build a roster, set recurring weekly hours by location, then watch a rolling two-week grid resolve them against approved time off, campus closures, and one-date overrides. Hand-drawn coverage strips flag every block that falls below minimum staffing.
